{% extends 'base.html.twig' %}{% block title %}Создать запись — CopyPaste{% endblock %}{% block body %}<section class="card"><h1>Передать текст безопасно</h1><p class="muted">Текст шифруется вашим ключом. Ключ не сохраняется на сервере.</p>{{ form_start(form) }}{{ form_errors(form) }}{{ form_row(form.text) }}{{ form_row(form.hint) }}{{ form_row(form.secret) }}<button type="submit">Зашифровать и получить ссылку</button>{{ form_end(form) }}</section>{% endblock %}
